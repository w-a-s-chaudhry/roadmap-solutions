[project]
name = "ecommerce-data-analysis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "matplotlib",
    "seaborn",
    "openpyxl",
    "notebook",
    "ipykernel",
]

[tool.uv]
package = false
