[project]
name = "titanic-eda"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "matplotlib",
    "seaborn",
    "notebook",
    "ipykernel",
]

[tool.uv]
package = false
